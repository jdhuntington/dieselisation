require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe Dieselisation::GameInstance do
  describe 'during the private auction' do
    it 'should automatically hand over a private to a single bidder if said private is the lowest ranked' do
      
    end
  end
end
